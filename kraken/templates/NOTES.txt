Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get {{ .Release.Name }}

An ingress controller is required to use Kraken on K8S: https://kubernetes.io/docs/concepts/services-networking/ingress/

Applications are available at:

- Gatling: http://{{ .Values.host.name }}/gatling/
- Administration: http://{{ .Values.host.name }}/administration/
- User Management (KeyCloak): http://{{ .Values.host.name }}/auth/
- Documentation: http://{{ .Values.host.name }}/doc/
