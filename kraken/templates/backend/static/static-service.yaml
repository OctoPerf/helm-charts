apiVersion: v1
kind: Service
metadata:
  name: {{ template "name" . }}-static
  labels:
    app: {{ template "name" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  selector:
    app: {{ template "name" . }}-static
  ports:
    - protocol: TCP
      port: {{ .Values.backend.static.port }}
      targetPort: {{ .Values.backend.static.port }}